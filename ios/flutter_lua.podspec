#
# To learn more about a Podspec see http://guides.cocoapods.org/syntax/podspec.html
#
Pod::Spec.new do |s|
  s.name             = 'flutter_lua'
  s.version          = File.read('../VERSION').chomp
  s.summary          = 'Lua interpreter and runtime.'
  s.description      = 'Lua interpreter and runtime for executing dynamic scripts in Flutter apps.'
  s.homepage         = 'https://github.com/drydart/flutter_lua'
  s.license          = { :file => '../UNLICENSE' }
  s.author           = { 'Arto Bendiken' => 'arto@bendiken.net' }
  s.source           = { :path => '.' }
  s.source_files = 'Classes/**/*'
  s.public_header_files = 'Classes/**/*.h'
  s.dependency 'Flutter'
  s.ios.deployment_target = '8.0'

  # Generated by GoMobile:
  s.vendored_frameworks = 'Frameworks/Flutter_lua_vm.framework'
  s.preserve_paths = 'Frameworks/Flutter_lua_vm.framework'
  s.xcconfig = { 'OTHER_LDFLAGS' => '-framework Flutter_lua_vm' }
end
